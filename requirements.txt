# Python deps for genai_test on a typical Linux HPC node.
#
# PyTorch: many clusters provide CUDA builds via environment modules or conda.
# If `pip install` is allowed, install a wheel that matches the node’s CUDA *before*
# this file, for example (pick cu* to match https://pytorch.org/get-started/locally/):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# If a cluster module already provides torch/torchvision, `pip install -r requirements.txt`
# will usually leave those satisfied; install any remaining lines the resolver reports.
#
# Do not pin a private PyTorch index here — air-gapped or module-based systems break on it.

numpy>=1.24
opencv-python-headless>=4.8
torch>=2.1
torchvision>=0.16
Pillow>=10.0
tqdm>=4.66
tensorboard>=2.14
torchmetrics>=1.4
lpips>=0.1.4
